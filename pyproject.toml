[project]
name = "relay-mcp"
version = "0.1.0"
description = "relay MCP server"
requires-python = ">=3.10"

[project.scripts]
relay-mcp = "server:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
